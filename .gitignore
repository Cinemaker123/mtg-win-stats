# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.*.local

# Screenshots
Bildschirmfoto*.png
Screenshot*.png

# Supabase data exports must never land in this (public) repo — the backup
# workflow pushes them to a separate private repo instead
backups/

# Local working notes (deferred cleanup / refactor backlog)
CLEANUP-BACKLOG.md

# Knowledge graph: fully local. graph.json was tracked, but the PostToolUse hook
# rebuilds it on every edit, so it went dirty every session for no review value.
# Regenerate with `graphify update .`; everything else here is local extraction
# state anyway. .claude/ is local hook wiring.
graphify-out/
.claude/

# Local MCP server wiring (personal, and the Supabase entry pins a project ref)
.mcp.json

# Playwright MCP session artifacts (throwaway page-state snapshots)
.playwright-mcp/
